# ripple-game-of-life

To install dependencies:

```bash
bun install
```

To build the Ripple bundle:

```bash
bun run build
```

To run locally:

```bash
bun run dev
```

This project was created using `bun init` in bun v1.3.5. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
